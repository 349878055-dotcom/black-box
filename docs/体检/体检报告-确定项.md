# 体检报告 · 确定项（已确认并已修复）

> 本文件记录「能 100% 确定」且已直接修复的问题。每项含文件:行号、问题、修复方式。
> 修复后已回归验证：`python3 -m compileall` 通过、`pyflakes` 收敛、skill 注册表 4 个正常加载。

---

## 一、未使用导入 / 未使用变量（共 12 处，已清理）

| # | 文件:行号 | 问题 | 修复 |
|---|-----------|------|------|
| 1 | [`registry.py`](cloud/cloud_orchestrator/adapters/registry.py:10) | `import json` 未使用 | 删除 |
| 2 | [`ws.py`](cloud/cloud_orchestrator/channel/ws.py:19) | `from typing import Any` 未使用 | 删除 |
| 3 | [`flows.py`](cloud/cloud_orchestrator/store/flows.py:12) | `from pathlib import Path` 未使用 | 删除 |
| 4 | [`tuniu/api/query.py`](cloud/cloud_orchestrator/store/archive_center/skill_archive/jintao/skills/tuniu/api/query.py:13) | `config.get` 兼容导入块整体未使用 | 删除整个 try/except 导入块 |
| 5 | [`glyy/api/login.py`](cloud/cloud_orchestrator/store/archive_center/skill_archive/jintao/skills/glyy/api/login.py:8) | `import json` 未使用 | 删除 |
| 6 | [`graph_engine.py`](cloud/cloud_orchestrator/core/graph_engine.py:260) | `SystemMessage` 导入未使用 | 从 import 中移除 |
| 7 | [`agent.py`](cloud/cloud_orchestrator/core/agent.py:11) | `import asyncio` 未使用 | 删除 |
| 8 | [`agent.py`](cloud/cloud_orchestrator/core/agent.py:15) | `typing.Any` 未使用 | 从 import 中移除 |
| 9 | [`routes.py`](cloud/cloud_orchestrator/api/routes.py:17) | 顶部 `handle_websocket` 导入未使用（函数内 [`websocket_endpoint()`](cloud/cloud_orchestrator/api/routes.py:348) 已自行导入） | 删除顶部冗余导入 |
| 10 | [`verify_cloud_service.py`](tools/verify_cloud_service.py:4) | `import base64` 未使用 | 删除 |
| 11 | [`diag_react.py`](tools/diag_react.py:15) | `SYSTEM_PROMPT_HIRED`、`ast` 未使用 | 从 import 中移除 |
| 12 | [`liance_test.py`](tools/liance_test.py:18) | `import sys` 未使用 | 删除 |
| 13 | [`deploy_cloud.py`](tools/deploy_cloud.py:65) | `j = child.expect(...)` 返回值未使用 | 改为直接调用，不赋值 |

## 二、依赖声明缺失（已补）

| # | 文件:行号 | 问题 | 修复 |
|---|-----------|------|------|
| 1 | [`requirements.txt`](requirements.txt:5) | 代码 `import requests`（[`tuniu/query.py`](cloud/cloud_orchestrator/store/archive_center/skill_archive/jintao/skills/tuniu/api/query.py:11)、[`tuniu/order.py`](cloud/cloud_orchestrator/store/archive_center/skill_archive/jintao/skills/tuniu/api/order.py:22)、[`njpkzyy/_base.py`](cloud/cloud_orchestrator/store/archive_center/skill_archive/jintao/skills/njpkzyy/api/_base.py:20)）但未声明 | 补 `requests>=2.31.0` |
| 2 | [`requirements.txt`](requirements.txt:20) | 代码 `import numpy`（[`retrieval/index.py`](cloud/cloud_orchestrator/retrieval/index.py:16)）直接使用但未显式声明 | 补 `numpy` |

## 三、资源管理不健壮（已修复）

| # | 文件:行号 | 问题 | 修复 |
|---|-----------|------|------|
| 1 | [`flows.py`](cloud/cloud_orchestrator/store/flows.py:52) | `open(...).write(...)` 临时文件对象无显式关闭，依赖 GC | 改为 `with open(...) as f: f.write(...)` 显式关闭 |

---

## 回归验证结果

- `python3 -m compileall -q cloud tools` → 通过（无语法错误）。
- `pyflakes cloud tools` → 仅剩：`store/*.py` 的 `import *` 兼容导出层（有意设计，见待定项说明）与 [`retrieval/index.py`](cloud/cloud_orchestrator/retrieval/index.py:245) 的 `cfg`（待定项）。
- skill 注册表实际加载 4 个，无加载失败：`glyy`(25)、`meituan_waimai`(6)、`njpkzyy`(9)、`tuniu`(14)。

---

## 四、方向1 契约一致性 · 附带发现（已修复）

| # | 文件:行号 | 问题 | 修复 |
|---|-----------|------|------|
| 1 | [`njpkzyy/api/api.py`](cloud/cloud_orchestrator/store/archive_center/skill_archive/jintao/skills/njpkzyy/api/api.py:27) | `__main__` 里同步调用 `async def list_depts()` / `generate_deep_link()`，未 `await`，直接运行即 `TypeError` | 改为 `asyncio.run(main())`，内部 `await` 调用两个 async 方法 |

## 五、方向2 API 逆向健壮性（已修复）

| # | 文件:行号 | 问题 | 修复 |
|---|-----------|------|------|
| 1 | [`glyy/api/_base.py`](cloud/cloud_orchestrator/store/archive_center/skill_archive/jintao/skills/glyy/api/_base.py:147) | `parse_response()` 全仓库 0 调用（实际解析走 `_parse_text`），死代码 | 删除该方法 |
| 2 | [`meituan_waimai/api/_base.py`](cloud/cloud_orchestrator/store/archive_center/skill_archive/jintao/skills/meituan_waimai/api/_base.py:68) | `parse_response()` 全仓库 0 调用，死代码 | 删除该方法 |

## 六、方向4 主代理编排（已修复）

| # | 文件:行号 | 问题 | 修复 |
|---|-----------|------|------|
| 1 | [`agent.py`](cloud/cloud_orchestrator/core/agent.py:677) | `_confirm_if_needed()` 用 `any(x in ans ...)` 判断确认，「不好/不是/不要/算了」会被「好/是」子串误判为确认，真操作（挂号/下单）确认可被绕过 | 否定词（不/否/取消/别/算了）优先排除 + 正向词前缀匹配（`startswith`），不再子串包含判断 |

## 七、挂号「时段匹配」致命问题（已修复）

| # | 文件:行号 | 问题 | 修复 |
|---|-----------|------|------|
| 1 | [`glyy/api/query.py`](cloud/cloud_orchestrator/store/archive_center/skill_archive/jintao/skills/glyy/api/query.py:38) | `get_schedule` 的 `detail` 是嵌套时段数组、时段元素不含医生字段，`requires` 无法按「医生+时段」匹配，下单会取第一个时段（挂错号） | 新增 `_flatten_schedule()` 展平：每个时段一条记录，doctor/detail 字段平铺到顶层 |
| 2 | [`glyy/contract/methods.json`](cloud/cloud_orchestrator/store/archive_center/skill_archive/jintao/skills/glyy/contract/methods.json:466) | `register` 的 `schedule_num_id` 只按 `doctor_name` 匹配；`start_hour` 有兜底会取第一个时段 | `match` 加 `time_part→start_hour`；删除 `start_hour` 兜底 requires；desc/params 明确「时段由客户选定后 AI 必传」 |
| 3 | [`agent.py`](cloud/cloud_orchestrator/core/agent.py:856) | `_extract_value()` 匹配到容器 dict（`{normal/expert}` 顶层）但取不到字段就 `return None`，导致 match 匹配失败 | 取到空值时 `continue` 找下一条真实记录，不再直接返回 |
| 4 | [`glyy/contract/methods.json`](cloud/cloud_orchestrator/store/archive_center/skill_archive/jintao/skills/glyy/contract/methods.json:402) | `register.customer_input` 为空，AI 不会主动 ask 缺的「科室名/医生名/日期/时段」，全靠 LLM 上下文提取，多轮可能漂移 | `customer_input` 补 `dept_name`/`doctor_name`/`appointment_time`/`start_hour`，缺则 ask_user 问清；desc 同步注明「禁止猜测」 |

## 八、角度A 安全与隐私（已修复）

| # | 文件:行号 | 问题 | 修复 |
|---|-----------|------|------|
| 1 | [`agent.py`](cloud/cloud_orchestrator/core/agent.py:152) | 登录意图日志记录**完整手机号**，泄露隐私 | 脱敏为「phone尾4位」，日志不再记完整手机号 |

- 验证：模拟「张医生 09:00 / 10:00 两个时段」数据，`_extract_value` 按「医生+时段」分别精确取到 `SN_9` / `SN_10`；缺时段时不误匹配返回 None。测试全部通过。

---

## 九、四方向循环第一轮（2026-08-19）

本轮按 [`四方向审核思路.md`](四方向审核思路.md) 扫描。纪律改为「点头后再改」，**本轮未动代码**。

已确认闭环、不翻案：

- 云端 `User.profile` 已删除（`users.py` / `MeUpdate` / `GET /me` 均无 profile）
- 会话 `persona` 已收口为 `person_id` / `person_name` / `skills`
- glyy 查询方法仍 `bearer=False`，未回潮
- Hired 闸门仍在：未雇人挂不上 `skill_run`；`confirm` 否定词优先；支付按返回内容弹出+留链接

确定错误见 [`体检报告-待定项.md`](体检报告-待定项.md)「待定 26 起」。

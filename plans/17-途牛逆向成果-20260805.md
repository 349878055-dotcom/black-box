# 途牛逆向成果（2026-08-05 收工记录）

## 一、两条路都摸清了

### 1. 途牛官方 MCP（已封装 [`tuniu_api.py`](../cloud/cloud_orchestrator/adapters/tuniu_api.py)，skill 已上线）

- **查票免费可用**：火车（search_train 30车次）、机票（10航班）、酒店、门票
- **下单受限**：`bookTrain` 依赖 `queryTrainDetail` 的 resId，但**当前 key 详情返回 null**（途牛权限分级，详情/下单需签约）
- 配置：`cloud/config.json` → `tuniu.api_key`（`sk-0f99...`，旧项目 .env 找到）
- 已注册 registry + flow 分层（查询为主）

### 2. 途牛官网逆向（本次重大突破）

**结论：带登录 cookies + 浏览器头 = 直接 requests 调接口，绕过 JS 反爬（202）。**

## 二、App「导出登录态」功能（新增）

- MainActivity `JsBridge.executeCmd` 新增 `export_cookies`：`CookieManager.getCookie(domain)`
- 写文件：`/sdcard/Download/tuniu_cookies.txt`（电脑 adb pull 读取）
- ui.html 设置页加「导出登录态（域名）」输入框 + 按钮，存 localStorage
- **途牛登录态已导出**：1314 字符（手机号 18913300200，userId 132133708，isLogin=true）

## 三、途牛官网火车票接口（已逆向通）

| 步骤 | 接口 | 参数 | 结果 |
|---|---|---|---|
| 拿 token | `POST /api/train/trainApi/getLocalVersion` | {} | token（如 d9d1...）+ version |
| 城市→代码 | `POST /api/train/product/fuzzySearch` | {keyword:"南京"} | cityCode:1602（北京:200） |
| **查车次** | `POST /api/train/product/ticketList` | departureCityCode/arrivalCityCode/departureCityName/arrivalCityName/departureDate | **78 车次**（D10 等，含 resId/price/seat/余票） |

- **API host**：`https://m-p.tuniu.com`（生产），POST + JSON，`Content-Type: application/json`
- **Referer**：`https://m.tuniu.com/train/`
- 请求头：浏览器 UA（Mobile Safari/Chrome）
- 关键返回字段：`trainId / trainNum / departStationCode / resId / price / seat / leftNumber / saleStatus`

### 其它已发现接口（index.js 逆向）
```
/api/train/user/userLoginStatusAjax     登录状态（isLogin/userId/cellphone）
/api/train/product/queryTrainStations   车站（参数未通，用 fuzzySearch 代替）
/api/train/product/stationCitys*        城市列表
/api/train/product/calendarV2           日历
/api/train/appProduct/auto12306Login    绑定 12306 登录（下单必需）
/api/train/appProduct/checkLogin12306   12306 绑定状态
/api/train/appProduct/getLoginRandCode  12306 验证码
/api/train/product/getMinistryRailwayByUserID  12306 绑定状态查询
/train/order/orderDetail / newOrderDetail / queryActivityOrderInfo  订单查询
```

## 四、⚠️ 下单/退票的关键卡点：需绑 12306

- `getMinistryRailwayByUserID` 返回 `loginStatus:0, userName:""` → **当前未绑定 12306**
- 途牛火车票购买走 12306 系统：先 `auto12306Login`（12306 账号+密码+验证码）绑定，再下单
- **用户要「买能退的票测试」需要 12306 账号**（用户暂时没有 → 收工）

## 五、下一步（用户回来后）

1. 用户提供 **12306 账号密码** → 摸 `auto12306Login` 绑定 → 摸下单/退票接口
2. 或先封装「途牛官网查票」（ticketList 已通，独立于 MCP）
3. 退票：途牛订单退票走 12306（`/train/order/*` + 12306）

## 六、代码/文件状态

- `cloud/cloud_orchestrator/adapters/tuniu_api.py`：MCP 封装（查票）+ book_train_auto（待 resId）
- `app/.../MainActivity.java`：export_cookies（写 /sdcard/Download/tuniu_cookies.txt）
- `app/.../assets/ui.html`：设置页「导出登录态」
- `/tmp/tuniu_cookies.txt`：途牛登录态（1314 字符，有效）
- `/tmp/tuniu_train_index.js`：途牛火车票 SPA JS（接口字典）

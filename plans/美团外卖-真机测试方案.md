# 美团外卖 · 真机测试方案（在 个人助理5 进行）

> ⚠️ **重要分工（用户铁令）**：
> - **工作台3.0** = 制作端（做 skill 三件套），**不连手机**，不在这做手机测试
> - **个人助理5**（`/home/jintao/桌面/个人助理5/`）= 消费端，**连手机 + 腾讯云**，手机测试都在这里做
> - 手机端 = 华为 + 虾米 App（AndroidBridge / 内置浏览器）；云端 = 腾讯云 `140.143.144.28`

---

## 一、环境
| 项 | 值 |
|---|---|
| 腾讯云 | `http://140.143.144.28`（公网 80 → nginx → 内部 19000） |
| 云端代码 | `/home/ubuntu/xiami/cloud/cloud_orchestrator/`（systemd：shimeban-cloud.service） |
| 手机端 | 个人助理5 `app/`（Android，ui.html：登录→连 WS→device_id=email） |
| 美团外卖 skill | `个人助理5/cloud/cloud_orchestrator/adapters/skills/meituan_waimai/`（已部署过一次） |

## 二、手机连接排查（为什么"总连不上"——已诊断）

**云端已实测正常**：
- HTTP 80 → 200 ✅
- WebSocket `ws://140.143.144.28/api/v1/ws?device_id=xxx` → 连接成功 ✅（nginx WS 代理正常）

**根因在手机端**（[`个人助理5/app/app/src/main/assets/ui.html`](/home/jintao/桌面/个人助理5/app/app/src/main/assets/ui.html:586)）：
```js
S.base || localStorage.getItem('xiami_base') || 'http://140.143.144.28'
```
- 手机 App 优先用 localStorage 存的 `xiami_base`；若存了旧地址（演示时的局域网 IP），手机连错地方
- 且[`ui.html:1040`](/home/jintao/桌面/个人助理5/app/app/src/main/assets/ui.html:1040) 设置页已移除，App 内改不了地址
- **解法**：手机清虾米 App 数据/重装 → `xiami_base` 回默认 `http://140.143.144.28` → 重新登录（登录会 set 正确 base + device_id=email + connectWS）

**验证手机在线**：腾讯云 SSH 看 `/tmp/cloud_orch.log` 的 `[bridge] 设备执行通道上线 device=<email>`，或 API `skill_run`（不在线返回"手机未在线"）。

## 三、真机测试流程（手机连上腾讯云后）

### 目标：验证「能不能下单」到提交订单前（不支付）+ 补全登录/下单 request

| 步 | 操作 | 说明 |
|---|---|---|
| 1 | 登录：`login_apply`（手机号 18913300200 发验证码） | 手机收短信；可能触发滑块 |
| 2 | `login`（验证码）→ token 存手机凭据库 | 抓真实 request 补全字段 |
| 3 | `search_poi`（搜奶茶/店铺） | 走手机通道（apimobile 403 需手机） |
| 4 | `get_poi_info` / `get_product_list`（看菜单） | 选店/选品 |
| 5 | `sync_cart` + `get_cart_info`（加购） | 拿 foodlist |
| 6 | `get_address_list`（地址） | 拿 recipient_* / addr_id |
| 7 | `preview_order`（结算预览） | 拿结算数据 |
| 8 | `submit_order`（**到提交前停，不支付**） | ⚠️ 真操作须确认；补全请求体（wm_poi_id/foodlist/风控 wxapp_base_data） |
| 9 | （可选）`get_orders` / `cancel_order` 闭环 | 验证副作用可回收 |

### 关键验证点
- 虾米手机通道（微信 UA + 手机真实 IP）能否过 passport 风控（**无 jsguard 设备指纹**）→ 登录
- 业务接口（i.waimai/apimobile）手机通道能否通
- `/order/submit` 用 `/order/submit` 还是 `/weapp/v1/order/submit`
- token 字段名 / 返回结构（`{"error":{...}}` vs 成功结构）

## 四、驱动方式（在个人助理5 / 腾讯云）
- 云端 API：`POST /api/v1/skills/meituan_waimai/run {method, params, device_id=<email>}`（需 auth token）
- 或手机虾米 App 对话："点外卖" → 主代理检索 → skill_run（手机可见聊天）
- 手机浏览器配合：`POST /api/v1/dev/browser {device_id, cmd, params}`（登录/滑块/看页面）

## 五、防封铁律
- **不要频繁探测美团接口**（风控会封，用户已提醒）
- 登录验证码 = 真人配合（收短信）；滑块/图形 = 真人过
- 真下单/退款前须用户确认
